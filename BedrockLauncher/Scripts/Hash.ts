import mcbeApi from "@mcbelauncher";
import bedrocklauncher from "@bedrocklauncher";
function hash(table, setter) {
    let args = { init: table, settings: {
        get: "*.arr",
        set: setter
    } };
}

function metaHash() {
    let collector = "installation";
    let get = "*.arr";
    let initialization = arg => {
        let wideInit = [500, 500];
    };
    launcher.autoLaunch(false);
}
